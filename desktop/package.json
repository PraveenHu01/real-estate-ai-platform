{
  "name": "real-estate-desktop",
  "private": true,
  "version": "0.0.0",
  "description": "Temporary Electron shell for running the platform as a local desktop app. Not deployed and not committed — see .git/info/exclude.",
  "main": "main.cjs",
  "scripts": {
    "start": "electron ."
  },
  "devDependencies": {
    "electron": "^43.4.0"
  }
}
